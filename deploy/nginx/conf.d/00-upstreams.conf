# Контейнеры приложения. Общие для http- и https-конфигурации,
# поэтому вынесены отдельно: при переключении на HTTPS файл не трогаем.

upstream web {
    server web:3000;
    keepalive 16;
}

upstream api {
    server api:4000;
    keepalive 16;
}
