# Публичный адрес API (браузер берёт отсюда фото /uploads/...)
NEXT_PUBLIC_API_URL=http://localhost:4000/api
# Адрес API для серверного рендеринга, если отличается (например, в Docker)
# API_URL=http://api:4000/api
